# 공식 AGENTS.md 문서 체인에 사용할 대체 파일명
project_doc_fallback_filenames = ["TEAM_GUIDE.md", ".agents.md"]
# 중첩 AGENTS 문서를 더 여유 있게 합칠 수 있도록 상향
project_doc_max_bytes = 65536

# 서브에이전트 공통 설정
[agents]
# 동시에 열 수 있는 최대 에이전트 수
max_threads = 6
# 하위 에이전트 깊이 제한
max_depth = 1
